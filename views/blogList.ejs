<!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
		<link rel = 'stylesheet' href = '/stylesheets/style.css' />
		<link rel = 'stylesheet' href = '/css/bootstrap.min.css' />
	</head>
	<body>
		<% include ./partials/nav %>
		<h1><%= title %></h1>
		<% if (blogs.length == 0) { %>
			<h4>No blogs to display.  Add some above.</h4>
		<% } else %>
		<% { %>
			<% let blog = {} %>
			<% for(let i = 0; i < blogs.length; i++) {
		      		blog = blogs[i] %>
				<% include ./partials/showBlog %>
			<% } %>
		<% } %>
	</body>
</html>
