<% formatDate(dateString) {
	var date = new Date(date);
	var result = date.getMonth() + ' ' + date.getDay() + ', ' + date.getFullYear() + ', ' + date.getHours() + ':' + date.getMinutes();
	return result;
    } %>
